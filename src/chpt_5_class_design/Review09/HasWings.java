package chpt_5_class_design.Review09;

public interface HasWings {
	// methods can be abstract, default or static. Therefore the statement "methods in interface are assumed to be abstract" is no longer 
	// valid since Java 8
	public abstract Object getWindSpan();

}
