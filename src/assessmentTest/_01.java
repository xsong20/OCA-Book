package assessmentTest;

public class _01 {
	/*
	 * Package:
	 * The prefix of a unique package name is always written in all-lowercase ASCII letters 
	 * and should be one of the top-level domain names, currently com, edu, gov, mil, net, org, 
	 * or one of the English two-letter codes identifying countries.
	 */
	
	/* Class:
	 * Class names should be nouns, in mixed case with the first letter of each internal 
	 * word capitalized. Try to keep your class names simple and descriptive.
	 * !!! Use whole words, avoid acronyms and abbreviations, unless unless the abbreviation 
	 * is much more widely used than the long form, such as URL or HTML).!!!!!!!
	 */
	
	/*Interface
	 * Names should be capitalized like class names.*/
	
	/* Methods 
	 * Names should be vexbs, in mixed case with the first letter lowercase,
	with the first letter of each internal word capitalized.
	*/
	/*
	 * Variable names should not start with underscore _ or dollar sign $ characters, 
	 * even though both are allowed.
	 */
	
	private static int $;
	public static void main(String[] args) {
		// a_b must be initialized
		String a_b= "11";
		System.out.println($);
		// a_b have not been initialized; Thus a compiler error in next line.
		System.out.println(a_b);
	}
	
	
}
